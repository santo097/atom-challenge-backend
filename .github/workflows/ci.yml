name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # -------------------------------------------------------------
  # Job 1: Análisis estático (lint + typecheck + build)
  # Rápido — corre primero, falla rápido si hay problemas básicos.
  # -------------------------------------------------------------
  static-analysis:
    name: Lint · TypeCheck · Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: TypeScript typecheck
        run: npm run typecheck

      - name: Build
        run: npm run build

  # -------------------------------------------------------------
  # Job 2: Tests unitarios con coverage
  # -------------------------------------------------------------
  unit-tests:
    name: Unit tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    env:
      NODE_ENV: test
      JWT_SECRET: test-secret-that-is-at-least-32-characters-long-for-zod
      JWT_EXPIRES_IN: 1h
      FIREBASE_PROJECT_ID: demo-atom-challenge
      CORS_ORIGINS: http://localhost:4200
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run unit tests with coverage
        run: npm run test:coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-unit
          path: backend/coverage/
          retention-days: 14

  # -------------------------------------------------------------
  # Job 3: Tests de integración contra Firestore emulator
  # Usa Java 21 (requerido por firebase-tools v15+).
  # -------------------------------------------------------------
  integration-tests:
    name: Integration tests (Firestore emulator)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    env:
      NODE_ENV: test
      JWT_SECRET: test-secret-that-is-at-least-32-characters-long-for-zod
      JWT_EXPIRES_IN: 1h
      FIREBASE_PROJECT_ID: demo-atom-challenge
      CORS_ORIGINS: http://localhost:4200
      FIRESTORE_EMULATOR_HOST: localhost:8080
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Setup Java 21 (required by Firestore emulator & firebase-tools v15+)
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Install backend dependencies
        run: npm ci

      - name: Run integration tests
        run: npm run test:integration
